Export created by Colab cell.
Contains: checkpoint state_dicts (*.pth) and per-coin scaler pickles (*.pkl).
Load state_dict in VS Code with model.load_state_dict(torch.load('...')) and use the corresponding coin scaler pickle for inverse-transforms.
